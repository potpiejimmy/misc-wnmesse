This folder contains:

the files and descriptors need for building an enterprise
ear file. By default, this will combine the "ejb" module and the
"war" module (see below) into the enterprise application.
